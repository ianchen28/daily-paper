# 故障排查指南

## 邮件发送问题

### 连接超时

**解决方案**：
1. 增加超时时间：在 `.env` 中设置 `SMTP_TIMEOUT=60`
2. 检查网络连接：测试 SMTP 服务器是否可达
3. 检查防火墙/代理设置
4. 尝试其他端口：如果 465 不行，尝试 587

### 认证失败

**解决方案**：
1. 确保使用的是授权码（不是登录密码）
2. 验证邮箱地址格式正确
3. 确保已启用 SMTP 服务（QQ 邮箱需要开启 POP3/SMTP 服务）

### 无法连接到 SMTP 服务器

**解决方案**：
1. 检查服务器地址和端口是否正确
2. 检查防火墙设置
3. 尝试其他邮箱服务商

## 其他问题

### LLM API 调用失败
- 检查 API Key 是否正确
- 检查 API 余额是否充足
- 检查网络连接

### 论文获取失败
- 检查网络连接
- 检查 HuggingFace API 是否可访问

### 报告内容为空
- 检查 LLM API 配置
- 查看日志中的错误信息

## 调试技巧

1. **查看详细日志**：程序已包含详细的日志输出
2. **查看生成的报告**：即使邮件发送失败，报告也会保存到 HTML 文件（`daily_paper_report_*.html`）
3. **检查配置**：确保 `.env` 或 GitHub Secrets 配置正确
